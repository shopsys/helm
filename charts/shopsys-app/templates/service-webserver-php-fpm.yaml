apiVersion: v1
kind: Service
metadata:
  name: webserver-php-fpm
  labels:
    {{- include "shopsys.labels" $ | nindent 4 }}
spec:
  selector:
    app: webserver-php-fpm
  ports:
    - name: http
      port: 8080
      targetPort: 8080
