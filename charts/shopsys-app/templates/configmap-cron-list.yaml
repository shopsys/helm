{{- if .Values.cron.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: cron-list
  labels:
    {{- include "shopsys.labels" $ | nindent 4 }}
data:
  cron: |+
    {{- include "shopsys.cronList.data" . | nindent 4 }}
{{- end }}
