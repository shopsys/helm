{{- if .Values.redis.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: redis
  labels:
    {{- include "shopsys.labels" $ | nindent 4 }}
data:
  redis.conf: |
    {{- .Values.redis.config | nindent 4 }}
{{- end }}
